---
summary: "CLI reference for `jar4 uninstall` (remove gateway service + local data)"
read_when:
  - You want to remove the gateway service and/or local state
  - You want a dry-run first
---

# `jar4 uninstall`

Uninstall the gateway service + local data (CLI remains).

```bash
jar4 uninstall
jar4 uninstall --all --yes
jar4 uninstall --dry-run
```

